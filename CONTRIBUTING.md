# Contributing

Install dependencies with `bun install`, then run `bun run check` before opening a pull request.

Keep credentials and durable Spacefast Link tokens on the server. Browser code may receive only short-lived frame sessions or host-signed grants.
